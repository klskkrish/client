import { CalculatedProductDto } from './calculated-product-dto';

describe('CalculatedProductDto', () => {
  it('should create an instance', () => {
    expect(new CalculatedProductDto()).toBeTruthy();
  });
});
